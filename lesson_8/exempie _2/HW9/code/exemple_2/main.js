// Объявляем массив из 10 элементов
const массив = [5, 3, 8, 1, 4, 9, 2, 6, 7, 0];

// Вырезаем первый элемент
const первыйЭлемент = массив.shift();

// Добавляем вырезанный элемент в конец массива
массив.push(первыйЭлемент);

// Выводим результат
console.log("Обновленный массив:", массив);